# torch is deliberately NOT pinned here: on x86 dev machines / the cluster,
# `pip install torch>=2.1` is correct. On Jetson (aarch64), PyPI has no CUDA
# build — install NVIDIA's JetPack-matched wheel FIRST (see JETSON_SETUP.md),
# then run this file with torch already satisfied.
torch>=2.1; platform_machine != "aarch64"
snntorch>=0.9
numpy
h5py
pyyaml
matplotlib
pillow

# optional, for export_onnx.py parity check / Jetson prep
# onnx
# onnxruntime
